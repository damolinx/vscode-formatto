{
  "name": "formatto",
  "displayName": "Formatto",
  "description": "A flexible Ruby formatter for VS Code, supporting rubyfmt, rufo and standardrb.",
  "author": "damolinx",
  "publisher": "damolinx",
  "license": "Apache-2.0",
  "version": "0.4.2",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/damolinx/vscode-formatto"
  },
  "bugs": {
    "url": "https://github.com/damolinx/vscode-formatto/issues"
  },
  "engines": {
    "vscode": "^1.105.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "categories": [
    "Formatters",
    "Programming Languages"
  ],
  "keywords": [
    "format",
    "formatter",
    "multi-root",
    "ruby",
    "rubyfmt",
    "rufo",
    "standardrb"
  ],
  "extensionPack": [
    "damolinx.tracciatto"
  ],
  "activationEvents": [
    "onLanguage:ruby"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "formatto.formatPendingChanges",
        "title": "Format Pending Changes",
        "category": "Formatto",
        "enablement": "gitOpenRepositoryCount > 0"
      }
    ],
    "configuration": [
      {
        "properties": {
          "formatto.enableRangeFormatting": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Enable **experimental** support of the **Format Selection** command (requires restart). `rubyfmt` does not support this natively so formatting may be inaccurate or differ from **Format Document**."
          },
          "formatto.formatter": {
            "type": "string",
            "default": "rubyfmt",
            "enum": [
              "rubyfmt",
              "rufo",
              "standardrb"
            ],
            "description": "Select which formatter Formatto should use."
          },
          "formatto.formatPendingChanges.includeStaged": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Include staged changes when running **Format Pending Changes**."
          }
        }
      },
      {
        "title": "Rubyfmt",
        "properties": {
          "formatto.rubyfmtArgs": {
            "type": "array",
            "default": [],
            "items": {
              "type": "string"
            },
            "markdownDescription": "Additional arguments to pass to `rubyfmt`, one argument per entry. Use `rubyfmt --help` to check for available flags."
          },
          "formatto.rubyfmtPath": {
            "scope": "resource",
            "type": "string",
            "default": "rubyfmt",
            "markdownDescription": "Path to the `rubyfmt` executable. The following tokens are available:\n\n- `${userHome}`: User home directory.\n- `${workspaceFolder}`: Current workspace root directory.\n\nIf the value is a bare command (e.g. `rubyfmt`), it will be resolved from the current environment's PATH.",
            "pattern": "^\\S*$",
            "patternErrorMessage": "Value must be empty, or a valid path"
          },
          "formatto.verifyRubyfmt": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Verify that `rubyfmt` is available at the resolved [path](command:workbench.action.openSettings?%5B%22formatto.rubyfmtPath%22%5D)."
          }
        }
      },
      {
        "title": "Rufo",
        "properties": {
          "formatto.rufoArgs": {
            "type": "array",
            "default": [],
            "items": {
              "type": "string"
            },
            "markdownDescription": "Additional arguments to pass to `rufo`, one argument per entry. Use `rufo --help` to check for available flags."
          },
          "formatto.rufoPath": {
            "scope": "resource",
            "type": "string",
            "default": "rufo",
            "markdownDescription": "Path to the `rufo` executable. The following tokens are available:\n\n- `${userHome}`: User home directory.\n- `${workspaceFolder}`: Current workspace root directory.\n\nIf the value is a bare command (e.g. `rufo`), it will be resolved from the current environment's PATH.",
            "pattern": "^\\S*$",
            "patternErrorMessage": "Value must be empty, or a valid path"
          },
          "formatto.rufoPreferBundler": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Prefer running `rufo` via `bundle exec` when a Gemfile is present and no explicit `formatto.rufoPath` is set."
          },
          "formatto.verifyRufo": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Verify that `rufo` is available at the resolved [path](command:workbench.action.openSettings?%5B%22formatto.rufoPath%22%5D)."
          }
        }
      },
      {
        "title": "Standard Ruby",
        "properties": {
          "formatto.standardrbArgs": {
            "type": "array",
            "default": [],
            "items": {
              "type": "string"
            },
            "markdownDescription": "Additional arguments to pass to `standardrb`, one argument per entry. Use `standardrb --help` to check for available flags."
          },
          "formatto.standardrbFormattingMode": {
            "type": "string",
            "enum": [
              "tmpFile",
              "forceSave"
            ],
            "enumItemLabels": [
              "Use temporary file",
              "Force-save editor"
            ],
            "default": "tmpFile",
            "markdownEnumDescriptions": [
              "Write the current editor contents to a temporary file, format and apply changes back.",
              "Force-save the current editor to disk before formatting."
            ],
            "markdownDescription": "`standardrb` can only format files on disk, so Formatto satisfies this requirement using the selected behavior whenever the target editor has pending changes."
          },
          "formatto.standardrbPath": {
            "scope": "resource",
            "type": "string",
            "default": "standardrb",
            "markdownDescription": "Path to the `standardrb` executable. The following tokens are available:\n\n- `${userHome}`: User home directory.\n- `${workspaceFolder}`: Current workspace root directory.\n\nIf the value is a bare command (e.g. `standardrb`), it will be resolved from the current environment's PATH.",
            "pattern": "^\\S*$",
            "patternErrorMessage": "Value must be empty, or a valid path"
          },
          "formatto.standardrbPreferBundler": {
            "type": "boolean",
            "default": false,
            "markdownDescription": "Prefer running `standardrb` via `bundle exec`."
          },
          "formatto.verifyStandardrb": {
            "type": "boolean",
            "default": true,
            "markdownDescription": "Verify that `standardrbPath` is available at the resolved [path](command:workbench.action.openSettings?%5B%22formatto.standardrbPath%22%5D)."
          }
        }
      }
    ]
  },
  "scripts": {
    "compile": "npm run ts:check-types && npm run ts:compile",
    "lint": "eslint \"src/**/*.ts\" --fix",
    "test": "node ./out/test/runTest.js",
    "ts:check-types": "tsc --noEmit",
    "ts:compile": "tsc -p ./",
    "ts:watch": "tsc -w -p ./",
    "vsce-package": "vsce package",
    "vscode:prepublish": "npm run ts:check-types && node esbuild.mjs --production",
    "watch": "npm run ts:watch"
  },
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "@stylistic/eslint-plugin": "^5.4.0",
    "@types/node": "^22",
    "@types/vscode": "~1.105",
    "@typescript-eslint/eslint-plugin": "^8.45.0",
    "@typescript-eslint/parser": "^8.46.0",
    "@vscode/test-electron": "^2.5.0",
    "@vscode/vsce": "^3.7.1",
    "esbuild": "^0.25.0",
    "eslint": "^10.2.1",
    "eslint-plugin-import": "2.32.0",
    "eslint-plugin-prettier": "^5.5.4",
    "prettier": "^3.6.2",
    "sinon": "^21.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.35.0"
  },
  "overrides": {
    "yauzl": "^3.2.1",
    "uuid": "^14.0.0"
  }
}